Cambia por el puerto por el tuyo en el archivo uploadcode.bat antes de ejecutar. y por la "Version del esp32" que uses.

intala lo que esta en requirements.txt con pip install -r requirements.txt

# Subelo y conectalo a la app BlE CONTROLLER


